<div class="jumbotron">
<h1>{{titulo}}</h1>
<p>{{desc}}</p>

<div class="card">
    <div class="card-body">
        <table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">ID</th>
      <th scope="col">NOMBRE</th>
      <th scope="col">DESC.</th>
     <th scope="col">ACCION</th>     
    </tr>
  </thead>
  <tbody>
    {{#each mascotasArray}}
    <tr>
      <th scope="row">{{this.id}}</th>
      <td>{{this.nombre}}</td>
      <td>{{this.descripcion}}</td>
      <td><a href="" class="btn btn-warning">Update</a>
      <a href="" class="btn btn-danger">Delete</a>
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>
</div>
<a class="btn btn-success  btn-block" href="/mascotas/new">New</a>
</div>
</div>